#include <iostream>
#include <sstream>


using namespace std;

int main()
{
	string line, token;
	int qtdL, sTo;
	
	while(true)
	{
		sTo = 0;
		
		cin >> qtdLi;
		cin.ignore();
		
		if(qLi == 0) break;
		else
		{
			getline(cin, line);
			
			issstream iss(line);
			
			
		}
	}
	
}
